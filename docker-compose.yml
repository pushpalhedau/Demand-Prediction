version: '3.8'

services:
  web:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
      - /app/.venv
      - /app/models
    environment:
      - DATABASE_URL=sqlite:////app/real_estate_demand.db
      - DEBUG=False
    restart: always
