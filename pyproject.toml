[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "predictax"
version = "2.0.0"
description = "PredictaX: multi-tenant demand intelligence platform for automobile dealer groups"
requires-python = ">=3.11"
# Runtime dependencies live in requirements/base.txt so Docker can install them without the source tree.
dynamic = ["dependencies"]

[tool.setuptools.dynamic]
dependencies = { file = ["requirements/base.txt"] }

[tool.setuptools.packages.find]
include = ["backend*", "frontend*"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
filterwarnings = ["ignore::DeprecationWarning"]
markers = [
    "unit: needs nothing external",
    "integration: needs a Postgres database",
    "e2e: needs the running services (auth, redis, worker)",
]

[tool.ruff]
line-length = 120
target-version = "py311"
extend-exclude = ["venv", "scripts/generate_*.py"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "S", "UP"]
ignore = [
    "B905",   # zip() without strict= is intentional throughout the analytics code
    "E501",   # long lines: formatter's job
    "S101",   # assert is fine in tests
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S", "B011"]
# st.set_page_config must be the first Streamlit call, so these entry points import after it.
"frontend/customer_app/main.py" = ["E402"]
"frontend/admin_console/main.py" = ["E402"]
